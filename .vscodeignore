# Development scripts — not needed in the extension
launch-vscode-error-screamer.sh
launch-vscode-error-screamer.ps1

# Tests — development only
test/
**/*.test.js

# Any generated/output VSIX files that may end up here
*.vsix

# Build artifacts folder
builds/

# Git / editor artifacts
.git
.gitignore
.DS_Store

# Internal dev / AI tooling config
.claude/
.vscode/
CLAUDE.md
TimePass.code-workspace

# Exclude banner from VSIX (icon.png is needed, banner is only for README/GitHub)
media/banner.webp

# Exclude ALL node_modules except the production dependency (sound-play)
node_modules/**
!node_modules/sound-play/
!node_modules/sound-play/**

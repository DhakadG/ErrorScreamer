# Dependencies
node_modules/

# Build artifacts (VSIX packages)
builds/

# Local VS Code workspace file
TimePass.code-workspace

# Claude Code files — AI tooling, machine-specific, not for the repo
.claude/
CLAUDE.md

# VS Code local IDE config — machine-specific, not for contributors
.vscode/

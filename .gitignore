# Dependencies
node_modules/

# Build artifacts (VSIX packages)
builds/

# Local VS Code workspace file
TimePass.code-workspace

# Claude Code local data (plans, memory, session settings — machine-specific)
.claude/
